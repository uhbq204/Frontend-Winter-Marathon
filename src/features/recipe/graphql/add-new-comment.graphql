mutation AddNewComment($input: CommentCreateInput!) {
    createComment(input: $input) {
        id
        content
        author {
            id
            avatarUrl
            profile {
                fullName
            }
        }
    }
}